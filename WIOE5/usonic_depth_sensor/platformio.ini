; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
extra_configs = platformio_secrets.ini

[common]
build_flags =
	-D WAKE_INTERVAL_MS=15000UL
	-D HEARTBEAT_INTERVAL_MS=2592000000UL
	-D SIGNIFICANT_CHANGE_THRESHOLD=0.05f
	-D LORAWAN_MAX_RETRIES=3
	-D LORAWAN_REGION=EU868
	-D __NO_INIT=

[env:lora_e5_mini]
platform = ststm32
board = lora_e5_mini
framework = arduino
build_flags = 
	${common.build_flags}
	${ttn.build_flags}
lib_deps =
	stm32duino/STM32duinoLoRaWAN@0.4.0
	stm32duino/STM32duino RTC@1.9.0
monitor_speed = 115200
upload_protocol = stlink
